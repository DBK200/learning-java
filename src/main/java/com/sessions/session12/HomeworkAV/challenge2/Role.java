package com.sessions.session12.HomeworkAV.challenge2;

public enum Role {
    ADMIN;
    enum User {
        MANAGER,
        CONSTRUCTOR
    }
    enum TeamPosition {
        MEMBER,
        LEAD
    }
}
